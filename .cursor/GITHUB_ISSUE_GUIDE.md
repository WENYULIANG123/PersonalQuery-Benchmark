# GitHub Issue 指南

## 什么是 GitHub Issue？

**GitHub Issue（问题/议题）** 是 GitHub 上用于跟踪和管理项目工作的工具。可以把它理解为：

- 📝 **任务清单**：记录需要完成的任务
- 🐛 **Bug 报告**：报告软件中的错误
- 💡 **功能建议**：提出新功能的想法
- ❓ **问题讨论**：讨论技术问题或设计决策
- 📋 **项目计划**：规划项目的工作项

## Issue 的特点

- **状态**：可以是 `open`（开放）或 `closed`（已关闭）
- **编号**：每个 issue 都有一个唯一的编号（如 #1, #2, #123）
- **标题和描述**：包含问题的详细说明
- **标签（Labels）**：用于分类（如 bug, enhancement, question）
- **分配（Assignees）**：可以分配给团队成员
- **评论（Comments）**：可以讨论和回复
- **关联 PR**：可以通过 PR 自动关闭 issue

## 如何获取 Issue？

### 1. 列出所有 Issues

```bash
# 列出开放的 issues
gh issue list

# 列出所有 issues（包括已关闭的）
gh issue list --state all

# 限制显示数量
gh issue list --limit 10

# 按标签筛选
gh issue list --label "bug"

# 按分配人筛选
gh issue list --assignee @me
```

### 2. 查看特定 Issue 的详情

```bash
# 查看 issue #123 的详情
gh issue view 123

# 查看 issue 并显示评论
gh issue view 123 --comments

# 以 JSON 格式输出
gh issue view 123 --json title,body,state,labels

# 在浏览器中打开
gh issue view 123 --web
```

### 3. 创建新 Issue

```bash
# 交互式创建
gh issue create

# 直接创建（带标题和内容）
gh issue create --title "修复登录问题" --body "描述问题..."

# 创建并分配标签
gh issue create --title "新功能" --body "..." --label "enhancement"
```

### 4. 在代码中引用 Issue

在提交信息或 PR 描述中使用关键词可以自动关联：

- `fixes #123` - 修复 issue #123（PR 合并时自动关闭）
- `closes #123` - 关闭 issue #123
- `resolves #123` - 解决 issue #123
- `refs #123` - 引用 issue #123（不自动关闭）

## 实际示例

### 查看当前仓库的 Issues

```bash
cd /home/wlia0047/ar57/wenyu
gh issue list
```

### 查看特定 Issue

```bash
# 假设有一个 issue #1
gh issue view 1

# 查看详细信息（JSON 格式）
gh issue view 1 --json number,title,body,state,createdAt,author
```

### 在 `/fix-issue` 命令中的使用

当使用 `/fix-issue 123` 命令时，系统会：

1. **获取 issue 详情**：
   ```bash
   gh issue view 123
   ```

2. **解析 issue 内容**：
   - 读取标题和描述
   - 了解需要修复的问题

3. **搜索相关代码**：
   - 在代码库中查找相关文件
   - 定位需要修改的代码

4. **实现修复**：
   - 根据 issue 描述修复问题
   - 编写或更新测试

5. **创建 PR**：
   - 提交更改
   - 创建引用 issue 的 PR（使用 `fixes #123` 或 `closes #123`）

## 常用命令总结

| 命令 | 功能 |
|------|------|
| `gh issue list` | 列出开放的 issues |
| `gh issue view <number>` | 查看特定 issue |
| `gh issue create` | 创建新 issue |
| `gh issue close <number>` | 关闭 issue |
| `gh issue reopen <number>` | 重新打开已关闭的 issue |
| `gh issue comment <number>` | 添加评论 |

## 更多资源

- [GitHub Issues 官方文档](https://docs.github.com/en/issues)
- [GitHub CLI 文档](https://cli.github.com/manual/gh_issue)
